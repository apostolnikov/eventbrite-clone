import * as React from 'react';

import RegisterForm from './register-form';
import LoginForm from './login-form';

export default () => (
	<React.Fragment>
		<RegisterForm />
		<LoginForm/>
	</React.Fragment>
);
