import * as React from 'react';

// import SearchBar from './search-bar';
import EventsList from './events-list';

export default () => (
	<React.Fragment>
		{/* <SearchBar/> */}
		<EventsList/>
	</React.Fragment>
);
